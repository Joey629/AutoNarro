# Slim runtime deps for Vercel (web API + static frontend).
# Full ML stack lives in requirements.txt for local training/inference.
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
psutil>=5.9.0
pandas>=2.0.0
